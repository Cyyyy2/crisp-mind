# Changelog

## 1.4.2 — 2026-09-13

- **节点内换行与多行编辑**：节点行内编辑器升级为自适应多行文本域（`<textarea>`），编辑文字时按 `⌘Enter` / `Ctrl+Enter` 即可在光标处插入换行，`Enter` 确认保存，`Esc` 取消。
- **编辑器动态自适应排版**：输入多行时编辑器随内容自动撑高与滚动限制，并同步画布缩放比例，保持与节点边界精准对齐；缩放与平移时动态重算几何位置。
- **大纲投影与数据持久化**：思维导图大纲投影（Markdown 列表）自动将节点换行规整为单行空格展示，保持大纲列表层级结构规范合法；底层 `.mind.md` 内嵌 JSON 数据完整保留真实换行并在重开、撤销与重做中保真，无任何内部几何字段泄漏。
- **快捷键作用域隔离**：通过 Obsidian Keymap 作用域精准接管 `Mod+Enter`，彻底解决上层全局快捷键抢先拦截换行输入的问题，并在提交与取消时安全注销。
- **帮助文档同步**：浮动工具栏帮助弹窗与快捷键列表同步补充节点内换行操作说明。

## 1.4.1 — 2026-09-12

- **付费授权门槛**：未激活时仅保留只读预览、平移缩放、搜索聚焦和大纲导航；新建导图、结构编辑、保存、样式备注、关系边界概要、布局主题、演示和导出均要求有效 Crisp 授权。
- **统一 Crisp 激活体验**：继续使用相同的 Ed25519 本地验签、设备在线核验和库内授权继承机制；触发受限功能时提示并直接打开 Crisp Mind 授权设置。
- **授权状态实时刷新**：激活或清除授权后，已打开的 Mind 视图会立即在只读预览与完整编辑之间切换。
- **大纲导航与折叠分支交互**：在大纲中点击折叠分支下的子节点时临时展开其路径并居中定位，清除不相关的分支聚焦；临时展开的分支支持点击折叠控件一键重新收起。
- **演示模式祖先节点临时显现**：播放演示步骤时若目标节点处于折叠分支，临时显现其祖先节点并在画布中居中，退出演示模式自动恢复折叠，不污染保存的文件折叠状态。
- **大纲搜索过滤全面遍历**：修复搜索过滤在兄弟分支判断中的短路问题，确保所有匹配分支完整显示。
- **多选批量删除焦点保护**：同时选中父子节点或多层祖先节点删除时，焦点平滑回退到真实存活的祖先节点。
- **导出视图同步展开状态**：SVG / PNG / PDF 导出正确识别大纲与演示的展开状态，完整呈现当前可见分支。

## 1.4.0 — 2026-09-12

- **节点样式与备注体系**：节点支持形状、填充色、文字色、边框色与粗细、字号、字重、对齐方式；备注随 `.mind.md` 保存，并在画布和大纲显示标记。分支复制会保留样式与备注。
- **关系、边界与概要**：支持跨分支关系箭头和标签、分支边界、阶段概要。概要默认针对选中主题的子分支，采用括弧收束与虚线引导标签，不再把父主题或普通子节点显示成概要。三者使用稳定节点 ID 引用，删除节点时自动清理失效标注，并随 SVG、PNG、PDF 导出。
- **多选与大纲侧栏**：支持 `⌘ / Ctrl + 点击` 追加选择、`Shift + 点击` 选择可见范围、批量应用样式和删除；新增可筛选的大纲侧栏，与画布选择、折叠和定位同步。
- **浮动面板交互调整**：单击节点只负责选中和显示快捷操作；节点样式检查器仅在点击工具栏或右键菜单中的“节点样式与备注”后打开。大纲与节点样式检查器统一改为不透明实底，并支持拖动标题栏移动位置。
- **边界与面板细节优化**：边界标签会在上下左右候选位置中选择与现有节点碰撞最少的位置，并用短引导线连接边界；边框粗细和字号改为 `− / 数值 px / +` 步进控件；大纲改为树状细线层级，折叠控件与连接线分列显示，移除节点行灰色胶囊底和无意义的叶子折叠块，并同步画布的展开 / 折叠状态。长节点文字最多显示两行并提供完整悬停提示，大纲支持按 1 至 5 级或全部层级查看。
- **右键菜单对齐**：节点右键菜单中的编辑、层级操作、关系、边界、概要、剪贴板和删除操作统一补齐图标，保持图标列与文字列对齐。
- **状态一致性**：分支聚焦、撤销 / 重做、批量删除和粘贴操作会同步刷新节点检查器与大纲选择；批量删除后选择真实的父节点，不再错误回退到根节点。
- **数据结构升级**：`.mind.md` 数据版本升级至 `1.1`，新增节点 `note`、`style` 与顶层 `relations`、`boundaries`、`summaries`，旧 `1.0` 文件继续兼容读取。

## 1.3.0 — 2026-09-12

- **节点搜索与分支聚焦**：工具栏新增搜索入口，可按节点文字查找并显示完整路径；点击结果后只聚焦相关分支，保留原导图的折叠结构，顶部可随时返回全图。
- **导图演示模式**：可选择节点并保存讲解顺序，逐步平移、缩放和突出当前节点；每一步可写只出现在编排界面的讲解备注，播放时使用方向键切换、Esc 退出。
- **Crisp Paper 纸感画布**：新增第六套主题，日间使用暖白纸面与细点纹理，夜间使用深石墨纸面；节点、分支、阴影和 SVG / PNG / PDF 导出同步采用同一套排版质感。
- **演示数据持久化**：演示顺序与备注随 `.mind.md` 保存并进入撤销 / 重做历史；已删除节点、重复步骤和无效引用会在读取时自动忽略。

## 1.2.4 — 2026-09-11

- **悬浮工具栏轻薄毛玻璃质感重塑**：外层容器剔除实色背景与厚边框，改用轻薄透明叠色直接借用窗口原生毛玻璃底色，与 Baseline 深度契合。
- **清除按钮静止态实色底块与内阴影**：严格作用域覆盖 Baseline 主题注入的按钮默认实色块与内阴影，静止状态图标保持高对比度纯净通透，悬停、点击与激活态保留轻量透明反馈。
- **规避 macOS 下导图 SVG 渲染重影**：工具栏移除多余的 `backdrop-filter: blur` 重复模糊层，彻底解决 macOS / Chromium 在底层思维导图 SVG 上因图层混合导致的文字合成重影。

## 1.2.3 — 2026-09-10

- 编辑框与节点的屏幕坐标、宽高、圆角和字号一致；取消独立的最小宽高和投影。
- 编辑期间隐藏旧节点框；超出可视边界时平移画布，避免输入框单独错位。
- 编辑框同步首次延迟适配及后续画布变换，修复输入位置滞留。
- 修复销毁编辑视图时 blur 清理与 DOM 移除重复执行的异常。
- 已在 125% 缩放与延迟适配场景下实测：编辑框与节点的位置、宽高保持一致，编辑中关闭不会残留异常。

## 1.2.2 — 2026-09-10

- **授权继承加固**：此前继承逻辑按固定顺序取仓库内**第一个**找到的授权码，不校验是否可用，也不在失败时尝试下一个——一个较早出现的、作用域不含 Crisp Mind 的单款授权会把后面可用的全家桶授权挡住。现在改为收集全部候选、逐个做本地签名与作用域校验，采用第一个**确实可用**的授权码。
- **继承只走本地校验**：候选扫描不再为每个候选发起一次在线设备核验（本地模式在密码学校验后即返回），最终采用的授权码仍照常执行在线设备核验，网络请求次数不增加。
- **未激活提示更具体**：仓库内找到授权但均不可用时，设置页会说明「找到 N 个授权，但均不可用于 Crisp Mind：<原因>」，而不是笼统显示未激活；单款授权的说明也明确了不会被继承。
- **测试夹具补齐 `TextDecoder`**：此前夹具未提供该全局对象，导致任何格式正确的授权码都会在解码载荷阶段提前返回，签名校验路径实际从未被测试覆盖。补齐后新增 5 项授权用例（本地校验、跨插件单款授权拒绝、候选去重与格式过滤、继承跳过不可用候选、无可用候选时返回空）。

## 1.2.1 — 2026-09-10

- **修复鱼骨图布局几何残留**：`cleanMindData` 此前只过滤 `_x / _y / _w / _h / _treeHeight / _treeWidth / _lines`，未包含鱼骨图专用的 `_isUpper`、`_spineConnectX/Y`、`_boneTipX/Y`、`_boneConnectX/Y`。由于换布局时只覆写 `_x/_y`、不清理旧字段，这些坐标会永久留在 `.mind.md` 中——实测单个文件因此多出约 18% 体积。现已全部排除，并补充针对六种布局与「从鱼骨图切回其它布局」的回归断言。
- **文档与实际一致化**：PDF 导出改称「2x 高清光栅化封装，非矢量」；删除「无外部网络依赖」这一与授权设备核验冲突的表述，改为明确披露核验时机会与请求内容；更正快捷键表中未实现的 `F2`、以及「适应画布」不存在键盘快捷键的描述；补充桌面端平台要求。

## 1.2.0 — 2026-09-10

- **新增六大多维布局体系**：
  - **逻辑结构图 (Logical Structure)**：经典左右单向/双向推演布局，紧凑自适应。
  - **思维导图 (Mind Map)**：根节点居中辐射，子节点智能均分至左右两侧。
  - **组织结构图 (Organization Structure)**：自顶向下层级展开，自动对齐同层级宽度。
  - **目录组织图 (Catalog Organization)**：纵向大纲缩进式层级展示。
  - **时间轴 (Timeline)**：水平发展主轴，里程碑节点交替上下错落分布，完美表达历史推演与阶段规划。
  - **鱼骨图 / 因果图 (Fishbone / Ishikawa)**：工业级根因分析图，配备鱼头几何指示、倾斜主骨与水平支骨自适应无碰撞布局算法。
- **高保真多格式导出引擎**：
  - **矢量 SVG 导出**：基于全局包围盒精准计算 viewBox，自带主题背景与高精度矢量曲线。
  - **2x 高清 PNG 导出**：支持 Retina 级像素倍率，内置白底纯净渲染，适合微信公众号、小红书与分享配图。
  - **高清 PDF 导出**：生成符合 PDF-1.4 标准的 PDF 文档（2x 高清光栅化封装，非矢量），保留真实排版与清晰度，便于文档归档与打印交流。
- **界面交互与体验打磨**：
  - **悬浮胶囊工具栏**：精炼 Apple 质感悬浮操作栏，整合自适应居中、布局切换、主题切换、导出与快捷键帮助。
  - **操作指南原生集成**：在工具栏中内置原生帮助按钮，一键调出完整的键盘快捷键与手势交互说明。
  - **全键盘流畅编辑**：原生支持 `Tab`（添加子节点）、`Enter`（添加同级节点）、`Space`（行内就地编辑）、`Delete`（删除分支）、`Cmd/Ctrl + Z / Shift + Z`（撤销与重做）。
  - **节点就近操作浮岛**：选中节点即时浮现快捷操作条，支持链接转换、待办切换、分支提取等高频动作。
- **5 套 Crisp 原创主题与原生深浅色自适应**：
  - 内置 `Crisp Obsidian`（自适应主题变量）、`Crisp Cupertino`（灰蓝）、`Crisp Mono Editorial`（当代编辑排版）、`Crisp Nord`（极光深暗）、`Crisp Amber Warm`（羊皮纸暖色）。
- **数据安全与契约自愈**：
  - 混合文件架构（`.mind.md`）：清晰可读的 Markdown 纯文本大纲结合隔离配置，支持全库文本搜索与双向链接。
  - 自动备份快照与冲突防覆盖保护，确保思考资产 100% 本地优先与安全可信。
- **Crisp Suite 统一授权体系**：
  - 基于 Ed25519 密码学校验，自动识别并继承库内其它 Crisp 插件的正版激活状态。
